<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".SensorReader">

    <mmbuw.com.sensorReader.DrawSomething
        android:id = "@+id/firstView"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_height="150dp" />

    <SeekBar android:id="@+id/scaleBar"
        android:max="100" android:layout_width="match_parent"
        android:layout_below="@id/firstView"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_marginTop="26dp" />

    <mmbuw.com.sensorReader.FFTView
        android:id = "@+id/fftView"
        android:layout_width="wrap_content"
        android:layout_below="@id/scaleBar"
        android:layout_centerHorizontal="true"
        android:layout_height="150dp" />

    <SeekBar android:id="@+id/scaleWindowBar"
        android:max="10" android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_marginTop="26dp" />

</RelativeLayout>
